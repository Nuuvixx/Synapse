# Synapse Backend Configuration

# =============================================================================
# REQUIRED
# =============================================================================

# OpenAI API Key for embeddings
OPENAI_API_KEY=sk-your-openai-api-key

# Database URL (PostgreSQL)
DATABASE_URL=postgresql+asyncpg://synapse:synapse@localhost:5432/synapse

# JWT Secret (generate with: openssl rand -hex 32)
JWT_SECRET=your-super-secret-jwt-key-change-in-production

# =============================================================================
# OPTIONAL - Vector Database
# =============================================================================

# Pinecone for vector storage (optional, uses in-memory if not set)
PINECONE_API_KEY=your-pinecone-api-key

# =============================================================================
# OPTIONAL - OAuth Providers
# =============================================================================

# GitHub OAuth
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret
GITHUB_REDIRECT_URI=http://localhost:3000/auth/github/callback

# Google OAuth
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:3000/auth/google/callback

# =============================================================================
# OPTIONAL - Server Configuration
# =============================================================================

# Server settings
PORT=8000
HOST=0.0.0.0

# Token expiration
ACCESS_TOKEN_EXPIRE_MINUTES=60
REFRESH_TOKEN_EXPIRE_DAYS=7

# Debug mode
DB_ECHO=false

# =============================================================================
# PHASE 6 - File Storage & Processing
# =============================================================================

# Storage Backend (local or s3)
STORAGE_BACKEND=local
UPLOAD_DIR=data/uploads
MAX_FILE_SIZE_MB=50
BASE_URL=http://localhost:8000

# S3/MinIO Configuration (required if STORAGE_BACKEND=s3)
S3_ENDPOINT=https://s3.amazonaws.com
S3_BUCKET=synapse-files
S3_ACCESS_KEY=your-s3-access-key
S3_SECRET_KEY=your-s3-secret-key
S3_REGION=us-east-1

# OCR Configuration
TESSERACT_CMD=/usr/bin/tesseract
GOOGLE_VISION_API_KEY=  # Optional, for cloud-based OCR

# CLIP Model for Image Embeddings
CLIP_MODEL=openai/clip-vit-base-patch32
